# Copy to project root as ".env" and fill in your key
# This file is ignored by git (see .gitignore)

OPENAI_API_KEY=sk-replace_me
# Optional override for default model when asking the agent
# OPENAI_MODEL=gpt-4o-mini

